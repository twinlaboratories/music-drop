# Required for /admin — share /admin?key=YOUR_SECRET with trusted people
ADMIN_SECRET=change-me-to-a-long-random-string

# RSVP capacity (optional, default 200)
RSVP_CAPACITY=200

# Optional: Upstash Redis so RSVPs persist on Vercel
# KV_REST_API_URL=
# KV_REST_API_TOKEN=
